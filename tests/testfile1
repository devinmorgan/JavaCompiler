====================Parser Tests====================
the tests either pass or they don't; there is no output

1) Program
    -- can only contain VarDecls, FuncDecls, ClassDecls, InterfaceDecls
    -- they can appear in any order throughout the program

2) VarDecl
    -- must follow a type
    -- the var itself is an identifier
    -- must be followed by ";"

3) types



///////////////////////////////////////////////////////

1) write a test for each possible one
-- one that is broken
-- 1 that works
2) write tests for objects that aren't allowed
-- int x = 5;
-- int reserverdWord = 5;
-- bad reserve words (i.e. wrong capitalization)
